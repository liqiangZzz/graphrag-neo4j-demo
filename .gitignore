# =====================================================================
# GraphRAG-Neo4j-Demo .gitignore
# =====================================================================
# 规则说明：
#   "忽略"  = 不需要提交到仓库
#   "提交"  = 需要提交到仓库
#
# 核心原则：
#   1. 个人环境 / 密钥 / 编译产物 / IDE 配置文件 —— 一律忽略
#   2. 源码、依赖清单、配置模板、data 数据 —— 必须提交
# =====================================================================

# ---------------------------------------------------------------------
# 密钥与敏感信息 —— 不提交（含有你的真实 API Key）
# ---------------------------------------------------------------------
.env

# 配置模板（不含任何真实密钥）—— 需要提交，勿忽略
!.env.example

# ---------------------------------------------------------------------
# 虚拟环境 —— 不提交（体积大且可由 requirements.txt 重建）
# ---------------------------------------------------------------------
.venv/
venv/
env/
ENV/

# ---------------------------------------------------------------------
# Python 编译产物 —— 不提交
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so

# 打包 / 构建产物
build/
dist/
*.egg-info/
.eggs/

# ---------------------------------------------------------------------
# IDE / 编辑器配置文件 —— 不提交（各人环境不同）
# ---------------------------------------------------------------------
.idea/
.vscode/
*.swp
*.swo

# ---------------------------------------------------------------------
# 操作系统杂项
# ---------------------------------------------------------------------
.DS_Store
Thumbs.db

# ---------------------------------------------------------------------
# 日志 / 临时文件
# ---------------------------------------------------------------------
*.log
*.tmp
*.temp

# ---------------------------------------------------------------------
# 测试与覆盖率产物
# ---------------------------------------------------------------------
.pytest_cache/
.coverage
htmlcov/